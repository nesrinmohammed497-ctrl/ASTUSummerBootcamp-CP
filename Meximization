t = int(input())
for _ in range(t):
    n = int(input())
    a = sorted(map(int, input().split()))
    ans = []
    dup = []
    for x in a:
        if not ans or ans[-1] != x:
            ans.append(x)
        else:
            dup.append(x)
    print(*(ans + dup))
